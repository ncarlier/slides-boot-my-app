#!/bin/bash

###
# Simple script to get a shell into the container.
###
docker run \
    --rm \
    --name="boot-my-app" \
    --env-file="./etc/boot-my-app.conf" \
    -i -t \
    --entrypoint="/bin/bash" \
    ncarlier/slides-boot-my-app \
    -c /bin/bash

