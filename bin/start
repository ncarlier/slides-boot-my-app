#!/bin/bash

###
# Simple script to launch the application.
# Start the server:
#   start
###
docker run \
    --rm \
    --name boot-my-app \
    --env-file="./etc/boot-my-app.conf" \
    -i -t \
    ncarlier/slides-boot-my-app $@

